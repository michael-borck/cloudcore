/.quarto/
/_site/
/_wip/.DS_Store
.DS_Store
._*
.__*
**/._*
**/.__*
backups/

**/*.quarto_ipynb
__pycache__/
*.pyc

# Instructor-only material — must never be pushed to the public repo
_instructor/

# Character persona ground-truth — students must discover this by interviewing
# the bots, not by reading the repo. Public repo: must stay untracked.
chatbots/_backstories/

# Design docs: scenario map and the artefact registry manifest (which flags
# which artefacts are planted distractors). Answer material — lives in the
# private simulation-staff repo. Same rule as _instructor/ and _backstories/.
scenario-analysis.md
config/registry-manifest.yml

# Tier-2 gated content: canonical in the private simulation-staff repo.
# scripts/build-site.sh copies it here for the full (VPS) render only.
docs/interviews.qmd
docs/interviews/
docs/logs.qmd
docs/support/logs/
docs/support/incident_report.md
docs/articles/data_breach_*.md
docs/support/letter_*.md
docs/support/network_logical.qmd
docs/support/erd.qmd
docs/policies/breach.md
docs/support/approved_software.md
_site-full/
